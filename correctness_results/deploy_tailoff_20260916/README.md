# 2026-09-16 关闭尾块细分的 A/B 实验

`logs/` 保存 small/big4 × A/B × 三次重复的原始日志及当时的汇总。
A 为 cgs_cross + pool，B 为 single + system 多进程。此实验关闭尾块细分，
并试图匹配两种部署的绝对 chunk 大小；具体 `-K`、rank 数和哈希配置以日志为准。

本目录中的 Python 是当时使用的分析快照；可维护版本在 `scripts/deploy_ab/`。
历史汇总可能由不完整的 rank 行计算，不能把缺失样本的观察最大值当成 makespan。
新解析器会拒绝将缺失/关闭/截短的哈希报告为有效全量指纹。
所有数字均是旧构建结果，不代表本次 LDM 保护修订后的性能。
