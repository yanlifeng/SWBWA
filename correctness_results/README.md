# Correctness Results

这里保存不同执行模式、输入模式和优化版本的正确性测试结果。

- 每个配置目录通常包含运行日志、状态文件、MD5 结果和必要的分析数据。
- `*.sam` 是较大的中间输出；验证通过后通常可以删除，避免结果目录膨胀。
- `.status`、`run.ok`、`md5.ok` 等文件记录增量测试脚本的完成状态。
- 目录名中的日期或实验名称用于区分不同代码版本和测试目的，不代表默认配置。
